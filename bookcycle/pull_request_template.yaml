pull_request_template:
  title: "📘 Bookcycle – Pull Request"

  sections:
    beschreibung:
      description: "Was wurde geändert?"

    bereich:
      - "Backend (Spring Boot)"
      - "Frontend (HTML/CSS/JS)"
      - "Admin App (Flutter)"
      - "Shared Resources"

    tests:
      - "Backend Tests laufen durch"
      - "Flutter Tests laufen durch"
      - "Frontend Linting ok"

    checklist:
      - "Keine unnötigen Dateien committed"
      - "Code formatiert"
      - "Lokaler Build erfolgreich"
      - "GitHub Actions erfolgreich"
