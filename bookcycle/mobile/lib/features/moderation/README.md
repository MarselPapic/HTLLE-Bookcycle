# moderation feature (mobile)

Scope: Reports and admin signals.

Layers:
- data: repositories, DTOs, API models
- domain: entities, value objects, use cases
- presentation: UI widgets, pages, state management
