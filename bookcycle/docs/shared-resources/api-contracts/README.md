# API Contracts (Shared)

This folder documents API contracts that are shared across bounded contexts.

Primary contract sources:
- `openapi/api-spec.yaml`
- `openapi/api-spec-identity.yaml`

Use this folder for cross-context notes, request/response examples, and migration notes without duplicating the OpenAPI source files.
